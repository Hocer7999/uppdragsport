export const siteConfig = {
    theme: "stadium-noir", // custom DaisyUI v5 dark theme defined in src/styles/global.css
    font: "inter" as "inter" | "dm-sans" | "manrope",
    siteType: "content" as "service" | "content" | "directory",
} as const;
